# راهنمای ویرایش فنی — کتاب «از PHP تا Go»

این سند راهنمای مرجع برای بازبینی و اصلاح فصول کتاب «از PHP تا Go» است. هر ویرایشگر باید این راهنما را به‌طور کامل مطالعه کند و قواعد آن را در تمام تصمیمات خود رعایت کند.

## ۱. قواعد ترجمه‌ی اصطلاحات فنی (حیاتی)

ترجمه‌ی اصطلاحات فنی باید یکدست باشد. جدول زیر ترجمه‌ی صحیح هر اصطلاح را مشخص می‌کند:

| انگلیسی | ترجمه‌ی صحیح | ملاحظات |
|---------|--------------|---------|
| Concurrency | هم‌روندی | مدیریت چند کار در یک بازه زمانی |
| Parallelism | هم‌زمانی | اجرای واقعی همزمان روی چند هسته |
| Goroutine | Goroutine | حفظ به‌صورت انگلیسی (نام专عی) |
| Channel | Channel (در متن انگلیسی) / کانال (در متن فارسی روان) | بسته به context |
| Interface | واسط | در متن فارسی؛ در کد به‌صورت انگلیسی |
| Pointer | اشاره‌گر | در متن فارسی |
| Slice | slice | حفظ به‌صورت انگلیسی |
| Map | map | حفظ به‌صورت انگلیسی |
| Garbage Collector | زباله‌روب | در متن فارسی؛ GC به‌صورت مخفف مجاز |
| Error | خطا | در متن فارسی؛ `error` در کد |
| Panic | panic | در متن فارسی هم به‌صورت انگلیسی |
| Race Condition | Race Condition | حفظ به‌صورت انگلیسی |
| Escape Analysis | Escape Analysis | حفظ به‌صورت انگلیسی |
| Stack | stack | در متن فارسی به‌صورت انگلیسی |
| Heap | heap | در متن فارسی به‌صورت انگلیسی |
| WaitGroup | WaitGroup | حفظ به‌صورت انگلیسی |
| Context | Context | حفظ به‌صورت انگلیسی (نام package) |
| Middleware | Middleware | حفظ به‌صورت انگلیسی |
| Embed | embed / جاسازی | بسته به context |

**نکته‌ی حیاتی:** در نسخه‌های قبلی، هر دو واژه‌ی «Concurrency» و «Parallelism» به «هم‌روندی» ترجمه می‌شدند که باعث سردرگمی می‌شد. ترجمه‌ی صحیح:
- Concurrency → هم‌روندی (ساختار و مدیریت چند کار)
- Parallelism → هم‌زمانی (اجرای واقعی همزمان روی چند هسته)

## ۲. قواعد حذف تکرار و ارجاع‌دهی متقابل

هر مفهوم باید فقط در یک فصل اصلی توضیح داده شود. در فصل‌های دیگر، به جای تکرار، باید ارجاع صریح داده شود.

### جدول محل اصلی مفاهیم

| مفهوم | فصل اصلی | فصل‌هایی که باید فقط ارجاع بدهند |
|-------|----------|----------------------------------|
| Goroutine | ۵.۱ | ۱.۶، ۱۰.۱، ۵.۷، ۵.۹ |
| Channel | ۵.۲ | ۱.۶، ۵.۷ |
| Select | ۵.۳ | ۵.۷ |
| Context | ۵.۴ | ۷.۱، ۹.۳، ۰۶.۰۷ |
| WaitGroup | ۵.۵ | ۵.۷، ۵.۸ |
| Race Condition | ۵.۶ | ۵.۱، ۱۱.۰۱ |
| Concurrency Patterns | ۵.۷ | ۵.۰۸ |
| Goroutine Leak | ۵.۹ | ۵.۰۱، ۱۰.۰۱ |
| Interface | ۳.۶ | ۱.۲، ۱.۳، ۰۲.۰۲، ۰۲.۰۶، ۰۳.۰۵، ۰۳.۰۷، ۱۱.۰۳ |
| Error Handling | ۴.۱ | ۱.۳، ۰۷.۱۱، ۰۸.۰۵، ۱۰.۰۰، ۱۱.۰۱ |
| panic | ۴.۰۳ | ۰۴.۰۱، ۰۵.۰۲، ۰۵.۰۵، ۰۷.۰۴ |
| Pointer | ۲.۰۵ | ۰۱.۰۶، ۱۲.۰۴ |
| Escape Analysis | ۲.۰۵ | ۰۱.۰۶، ۰۲.۰۴، ۰۳.۰۳، ۱۰.۰۰، ۱۲.۰۱، ۱۲.۰۴ |
| Garbage Collector | ۱۲.۰۴ | ۰۱.۰۶، ۰۲.۰۱، ۰۲.۰۵، ۰۳.۰۳، ۰۳.۰۴، ۱۰.۰۱ |
| GMP Scheduler | ۵.۰۱ | ۰۱.۰۶، ۰۵.۰۰ |
| Middleware | ۷.۰۴ | ۰۷.۰۱، ۰۷.۰۲، ۰۹.۰۱، ۱۱.۰۱ |
| embed | ۰۶.۰۵ | ۰۷.۰۸، ۰۷.۱۰، ۰۷.۱۲ |
| Migration | ۰۸.۰۱ | ۰۸.۰۲ |

### الگوی ارجاع‌دهی صحیح

وقتی مفهومی در فصل غیراصلی ظاهر می‌شود:

**قبل (تکرار):**
```html
<p>Goroutine واحد سبک‌وزن اجرا در Go است که فقط ۲ کیلوبایت حافظه مصرف می‌کند و توسط زمان‌بند داخلی Go مدیریت می‌شود...</p>
```

**بعد (ارجاع):**
```html
<p>Goroutine (که در <a href="05.01-goroutine.html">بخش ۵.۱</a> به‌تفصیل معرفی شده) واحد سبک‌وزن اجرا در Go است.</p>
```

یا در چکیده:
```html
<p>برای آشنایی عمیق با Goroutine به <a href="05.01-goroutine.html">بخش ۵.۱</a> مراجعه کنید.</p>
```

## ۳. قواعد ویرایش و فرمت‌بندی

### ۳.۱. ساختار هر فایل

هر فایل HTML باید این ساختار کلی داشته باشد:
- عنوان `<h2>` (عنوان بخش)
- چکیده (در `<div class="abstract">`)
- محتوای اصلی با `<h3>` برای زیربخش‌ها
- خلاصه‌ی نکات کلیدی (در `<div class="summary">`)

### ۳.۲. قواعد پاراگراف‌نویسی

- هر پاراگراف باید ۳ تا ۵ جمله داشته باشد (حداقل ۱۵۰ کاراکتر)
- جملات تک‌کلمه‌ای ممنوع (به‌جز جملات گذاری)
- هر بخش باید حداقل ۱۵۰-۲۰۰ کلمه محتوای بدنه داشته باشد
- از bullet list فقط برای فهرست‌های واقعی استفاده شود، نه برای جایگزینی پاراگراف

### ۳.۳. حذف کلمات زائد (Filler)

این الگوها باید حذف یا جایگزین شوند:
- «همان‌طور که می‌دانیم» → حذف
- «همان‌طور که می‌بینید» → حذف
- «همان‌طور که گفتیم» → حذف یا تبدیل به «پیش‌تر اشاره شد»
- «بگذارید» → حذف
- «به‌عنوان مثال» → «برای نمونه،»
- «حالا» (در ابتدای جمله) → حذف یا تبدیل به «اکنون»

### ۳.۴. شماره‌گذاری بخش‌ها

عناوین `<h3>` باید با شماره‌گذاری بخش (مثلاً «۵.۰.۱») شروع شوند. این الگو در فایل‌های موجود رعایت شده و باید حفظ شود.

### ۳.۵. متن کدها

- کدها باید داخل `<figure class="code [language]" dir="ltr">` باشند
- نظرات داخل کد به زبان انگلیسی یا فارسی مجاز هستند
- کدها باید معتبر و قابل اجرا باشند

### ۳.۶. نقل‌قول‌ها

نقل‌قول‌ها در `<div class="quote-box">` قرار می‌گیرند. ساختار:
```html
<div class="quote-box">
<p class="quote-text">متن نقل‌قول</p>
<p class="quote-source">
<span class="quote-author">نام</span>
<span class="quote-role">عنوان</span>
<span class="quote-separator">|</span>
<span class="quote-book">منبع</span>
</p>
</div>
```

### ۳.۷. یادداشت‌ها و نکات

- `<div class="note">` برای یادداشت‌های عمومی
- `<div class="tip">` برای نکات کاربردی
- `<div class="important">` برای هشدارهای مهم
- `<div class="warning">` برای هشدارهای امنیتی

## ۴. قواعد محتوایی

### ۴.۱. عمق محتوا

- هر مفهوم باید با مثال عملی همراه باشد
- مقایسه با PHP باید مشخص باشد (وقتی مرتبط است)
- کدها باید واقعی و قابل اجرا باشند
- توضیحات باید «چرایی» را پوشش دهند، نه فقط «چگونه»

### ۴.۲. متناقضات و اشتباهات فنی

برای هر اشتباه فنی:
1. ابتدا متن فعلی را بخوانید و بفهمید چه می‌گوید
2. با منابع معتبر Go بررسی کنید که آیا صحیح است
3. اگر اشتباه است، آن را اصلاح کنید
4. اگر متناقض است، نسخه‌ی صحیح را保留 کنید و دیگری را حذف یا اصلاح کنید

### ۴.۳. حفظ لحن

- لحن: فنی، دقیق، اما قابل فهم
- مخاطب: توسعه‌دهنده‌ی PHP که در حال یادگیری Go است
- از لحن گفتاری پرهیز کنید (مثل «بذارید»، «می‌کنیم» در جاهای غیرضروری)
- جملات باید خبری و رسمی باشند

## ۵. قواعد کار با فایل‌ها

### ۵.۱. حفظ ساختار HTML

- ساختار کلی فایل (head, header, main, footer) را تغییر ندهید
- فقط محتوای داخل `<main>` را ویرایش کنید
- تگ‌های BDI و dir="ltr" برای کلمات انگلیسی باید حفظ شوند
- تگ‌های inline (code, strong, em, bdi) باید حفظ شوند

### ۵.۲. کار با MultiEdit

برای ویرایش‌های متعدد در یک فایل، از ابزار MultiEdit استفاده کنید. هر edit باید:
- `old_str` دقیقاً با متن موجود مطابقت کند (شامل فاصله‌ها)
- `new_str` نسخه‌ی اصلاح‌شده باشد
- تغییرات به ترتیب اعمال می‌شوند

### ۵.۳. کار با Edit

برای تغییرات ساده، از Edit استفاده کنید. `old_str` باید در فایل یکتا باشد.

## ۶. ساختار گزارش کار

پس از اتمام کار، باید گزارشی با ساختار زیر در `/home/z/my-project/worklog.md` اضافه کنید:

```markdown
---
Task ID: <شناسه>
Agent: <نام>
Task: <توضیح مختصر>

Work Log:
- <گام ۱>
- <گام ۲>
- ...

Stage Summary:
- <تعداد فایل ویرایش‌شده>
- <تعداد اشتباه فنی اصلاح‌شده>
- <تعداد تکرار حذف‌شده>
- <تعداد ارجاع متقابل اضافه‌شده>
- <نکات مهم>
```

## ۷. مثال‌های عملی

### مثال ۱: حذف تکرار

**فایل 01.06-program-execution-overview.html** (بخش GMP)

این فایل توضیحات عمیقی درباره‌ی GMP دارد که تکراری با فصل ۵.۱ است. باید:

**قبل:**
```html
<h3>1.6.2 مدل GMP</h3>
<p>زمان‌بند Go از مدل GMP استفاده می‌کند — از مهم‌ترین مفاهیم داخلی Go که تأثیر مستقیم بر عملکرد برنامه دارد:</p>
<p>G (Goroutine): هر goroutine یک ساختار داده‌ی داخلی است (حدود ۲ کیلوبایت در ابتدا) شامل stack، instruction pointer، اطلاعات debugging و غیره...</p>
<p>M (Machine): نشان‌دهنده‌ی یک thread واقعی سیستم‌عامل است...</p>
<p>P (Processor): یک منبع مجازی است که نماینده‌ی یک هسته‌ی منطقی CPU است...</p>
<!-- چندین پاراگراف توضیحی دیگر -->
```

**بعد:**
```html
<h3>1.6.2 مدل GMP</h3>
<p>زمان‌بند Go از مدل GMP (Goroutine-Machine-Processor) استفاده می‌کند که در آن Goroutine‌های سبک روی Thread‌های واقعی سیستم‌عامل نگاشت می‌شوند. این مدل که تأثیر مستقیم بر عملکرد برنامه دارد، در <a href="05.01-goroutine.html">بخش ۵.۱</a> به‌تفصیل بررسی شده است. در اینجا فقط به اصول اولیه می‌پردازیم:</p>
<p>G (Goroutine) واحد سبک اجرا، M (Machine) یک Thread واقعی سیستم‌عامل، و P (Processor) یک منبع مجازی است که تعدادش برابر با GOMAXPROCS تنظیم می‌شود. این معماری به Go اجازه می‌دهد ده‌ها هزار Goroutine را به‌طور کارآمد مدیریت کند.</p>
<p>برای درک عمیق‌تر work-stealing، asynchronous preemption و تنظیم GOMAXPROCS در محیط‌های کانتینری، به <a href="05.01-goroutine.html">بخش ۵.۱</a> مراجعه کنید.</p>
```

### مثال ۲: اصلاح اشتباه فنی

اگر متنی می‌گوید «در PHP ۸، type hinting اختیاری است» (که اشتباه است):
```html
<!-- قبل -->
<p>در PHP ۸، type hinting کاملاً اختیاری است و می‌توانید از آن صرف‌نظر کنید.</p>

<!-- بعد -->
<p>در PHP ۷ به بعد، type declarations به‌طور گسترده پشتیبانی می‌شوند و در PHP ۸ با اضافه‌شدن union types و mixed type، سیستم نوع‌دهی قوی‌تر شده است. با این حال، همچنان می‌توانید از نوع‌دهی صرف‌نظر کنید (به‌جز برای پارامترهای خاص).</p>
```

### مثال ۳: بهبود ویرایش

**قبل:**
```html
<p>خب حالا بذارید درباره‌ی Goroutine صحبت کنیم. همون‌طور که می‌بینید Goroutine خیلی مهمه.</p>
```

**بعد:**
```html
<p>Goroutine واحد هم‌روندی Go است و در <a href="05.01-goroutine.html">بخش ۵.۱</a> به‌طور مفصل بررسی شده است. در ادامه، اصول اولیه‌ی آن را مرور می‌کنیم.</p>
```

## ۸. فهرست فایل‌های اولویت‌دار

برای ویرایش عمیق، این فایل‌ها بیشترین مشکل دارند:

1. **01.06-program-execution-overview.html** — تکرار GMP، GC، signal handling
2. **01.01-philosophical-contrast.html** — بنچمارک‌های مفصل (به ۱۲.۰۱ منتقل شود)
3. **01.04-tools.html** — مقایسه‌ی عمیق Composer vs go mod (به ۰۳.۰۲ منتقل شود)
4. **11.01-testing-basics.html** — بیش از ۵۴۰۰ کلمه (خیلی طولانی)
5. **05.00-chapter-5.html** — مشکل Concurrency/Parallelism (اصلاح شد)
6. **07.08 تا 07.13** — تکرار مطالب embed و template
